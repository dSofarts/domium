plugins {
    id 'idea'
}

subprojects {
    group = 'ru.domium'
    version = "$appVersion"
    
    repositories {
        mavenCentral()
    }

    tasks.withType(JavaCompile).configureEach {
        options.compilerArgs.add('-parameters')
    }
}

